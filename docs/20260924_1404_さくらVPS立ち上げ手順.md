# さくらVPS 立ち上げ手順

作成日: 2026-09-24

[公開までのタスク分割](20260923_1740_公開までのタスク分割.md) の **T7（VPS 構築・公開）** の手順書。
契約済みで停止しているさくらのVPS（2GB）を、「公開の直前」の状態まで立ち上げる。

何をやるか・なぜやるかは [プロジェクト方針とやりたいこと整理](20260912_1528_プロジェクト方針とやりたいこと整理.md) §3（ドメイン）・§5（VPS）が正。
構成は [ローカル docker compose 検証手順](20260922_1716_ローカルdocker-compose検証手順.md) と
[検証結果](20260922_2100_ローカルdocker-compose検証結果.md) で手元で確かめたものを、そのまま VPS に持っていく。

---

## 1. 前提と方針

### 前提

- **本番はまだ動いていない。** 移すデータは無く、**VPS の DB は空から作る**
  （マイグレーション＋必要なマスタ＋管理者1人）。
  方針ドキュメントや検証手順にある「本番データでの予行演習」「本番（Supabase Cloud）の設定確認」は、
  存在しない本番を前提にした記述なので、この手順では行わない（§7 で消す）
- seed（`packages/seed/main/data.ts`）の議案は架空のサンプルなので、**VPS には流さない**。
  会派・委員会・タグは実データなので、マスタとして使う（手順8）
- VPS は **さくらのVPS 2GB**。契約済みで、今は停止している

### 方針

- **立ち上げと公開を分ける。** T1（名称「デジ民」の商標）の判断が付くまでは公開しない。
  それまでは web・admin の手前の nginx で Basic 認証をかけ、関係者しか入れない状態で立ち上げる（手順9）
- **VPS 上でビルドしない。** web・admin は GitHub Actions（最初の1回は手元）でビルドし、成果物を rsync で送る
- **VPS の上で試行錯誤しない。** 設定ファイルはリポジトリの `infra/` に置き、PR でレビューしてから持っていく
- **2個目のプロジェクトを載せやすくする。** Supabase（Postgres）は共通基盤、アプリはプロジェクトごとのディレクトリと
  systemd unit、nginx は vhost を足せば増える形にする

---

## 2. 全体像

```
                     ┌─────────────── さくらのVPS（Ubuntu 24.04）───────────────┐
ブラウザ ──443──→ nginx（ホスト、TLS 終端、vhost）                              │
                   ├ gikai.ezocivic.tech       → 127.0.0.1:3004  web   (systemd)  │
                   ├ gikai-admin.ezocivic.tech → 127.0.0.1:3003  admin (systemd)  │
                   └ db.ezocivic.tech          → 127.0.0.1:8000  ┐                │
                                                                  ▼                │
                              docker compose（infra/compose.yml）                  │
                              nginx :8000 ─┬ /rest/v1/    → rest (PostgREST)       │
                                           ├ /auth/v1/    → auth (GoTrue)          │
                                           └ /storage/v1/ → storage (storage-api)  │
                                                            ↓                      │
                                                           db (Postgres 17)        │
                                                           127.0.0.1:5433          │
                     └─────────────────────────────────────────────────────────────┘

手元（WSL）── ssh -L 5433:127.0.0.1:5433 ──→ マイグレーション・psql
GitHub Actions ── ビルド → rsync ──→ /srv/gikai/{web,admin}
```

- **nginx は2段**（ホストの nginx → compose の nginx）。compose はローカル検証と同じものを使い回せる。
  そのかわり、利用者の IP を compose 側に正しく引き継ぐ設定が要る（手順7・§6）
- 外から届くのは 22 / 80 / 443 だけ。compose のポートと web・admin は `127.0.0.1` でしか待ち受けない

### ディレクトリ

```
/srv/
├── supabase/            # 共通基盤。リポジトリの infra/ をそのまま置く
│   ├── compose.yml
│   ├── .env             # 秘密情報（600）
│   ├── nginx/ db/
│   └── volumes/         # DB と画像ファイルの実体
├── gikai/               # プロジェクトごと
│   ├── web/             # standalone の成果物（rsync で置き換わる）
│   ├── admin/
│   ├── web.env          # web の実行時の環境変数（600）
│   └── admin.env
└── backups/             # pg_dump と画像の tar（手順11）
```

---

## 3. 分担

| 誰が | 何を |
|---|---|
| **あなた（人）** | さくらのコントロールパネル（起動・OS インストール・SSH 鍵・パケットフィルター）、DNS レコード、秘密情報の生成と GitHub Secrets への登録、OpenAI のハードリミット |
| **Claude（PR）** | §6 のコードと設定ファイル（`infra/` の追加、デプロイ用ワークフロー、systemd unit、バックアップスクリプト、アプリ側の修正） |
| **一緒に（SSH）** | 手順2〜12 のサーバー作業。コマンドは本書のとおり。Claude が SSH で入る場合は、鍵とホストをあなたが用意する |

**進める順番**: 手順1〜6（サーバーの土台）は §6 の PR を待たずに進められる。
手順7 以降は §6 の PR がマージされてから行う。

---

## 4. 手順

### 手順1: コントロールパネルでの作業（人）

1. **OS をインストールする**: コントロールパネルのサーバー詳細から OS の再インストール（表記は画面に従う）を選び、
   **Ubuntu 24.04 LTS（amd64）** を入れる
   - 「スタートアップスクリプト」は使わない（何が入ったか分からなくなるため）
   - 管理ユーザーのパスワードは長いものにする（SSH ではパスワードを使わないが、コンソールからのログインで使う）
   - **SSH 公開鍵を登録する**（手元の WSL の `~/.ssh/id_ed25519.pub`。無ければ `ssh-keygen -t ed25519` で作る）
2. **パケットフィルターを設定する**: 許可するのは **SSH（22）と Web（80 / 443）だけ**
3. インストールが終わるとサーバーは起動する。**IPv4 アドレス**（と IPv6 アドレス）を控える
4. 契約内容を控えておく（方針 §5 の宿題）: 月額、ゾーン（石狩か東京か）、
   **プランを上げる時にデータが残るか**（さくらのサポート情報で確認する）

### 手順2: 初回ログインと SSH の締め付け

```bash
# 手元（WSL）から。ユーザー名はコントロールパネルに表示されるもの（Ubuntu は通常 ubuntu）
ssh ubuntu@<IPv4>
```

```bash
sudo apt update && sudo apt full-upgrade -y
sudo timedatectl set-timezone Asia/Tokyo
timedatectl            # "System clock synchronized: yes" と "NTP service: active" を確認
sudo hostnamectl set-hostname ezocivic-vps1
```

時刻の同期は必ず確かめる。web のキャッシュの無効化は時計のずれに弱い（検証結果 §4-8）。

SSH をパスワードで入れないようにする:

```bash
sudo tee /etc/ssh/sshd_config.d/99-hardening.conf <<'EOF'
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin no
EOF
sudo sshd -t && sudo systemctl reload ssh
```

**今のセッションを閉じずに**、別のターミナルから `ssh ubuntu@<IPv4>` で入れることを確かめてから次に進む。

手元の `~/.ssh/config` に足しておくと以降が楽になる:

```
Host ezocivic-vps1
  HostName <IPv4>
  User ubuntu
```

### 手順3: ファイアウォールと自動更新

パケットフィルター（さくら側）に加えて、サーバーでも絞る。

```bash
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow OpenSSH
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
sudo ufw status verbose
```

> **docker のポートは ufw をすり抜ける。** docker は iptables を直接書き換えるので、compose で `0.0.0.0` に公開したポートは
> ufw で閉じていても外から届く。`infra/compose.yml` は `127.0.0.1` にだけ公開しているので問題ないが、
> ポートを足す時は必ず `127.0.0.1:` を付ける。

セキュリティ更新を自動で入れる:

```bash
sudo apt install -y unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades   # Yes
```

再起動が要る更新（カーネル）は自動では再起動しない。月に一度くらい `cat /var/run/reboot-required` を見て、手で再起動する。

### 手順4: スワップ

2GB のメモリに対し、実測は約 660MiB（検証結果 §4-7）。余裕はあるが、Postgres の伸びと Next.js の一時的な膨らみに備えて 2GB 足す。

```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
echo 'vm.swappiness=10' | sudo tee /etc/sysctl.d/99-swap.conf
sudo sysctl --system
free -h
```

### 手順5: Docker と Node.js を入れる

**Docker Engine**（公式の apt リポジトリ。Ubuntu の `docker.io` パッケージは古いので使わない）:

```bash
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] \
https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" \
  | sudo tee /etc/apt/sources.list.d/docker.list
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
sudo usermod -aG docker ubuntu     # 入り直すと sudo なしで docker が使える
```

docker のログが際限なく増えないようにする:

```bash
sudo tee /etc/docker/daemon.json <<'EOF'
{ "log-driver": "json-file", "log-opts": { "max-size": "10m", "max-file": "3" } }
EOF
sudo systemctl restart docker
```

**Node.js**（web・admin の `server.js` を動かすため。ビルドはしない）:

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
node -v
```

> **Node のバージョンは CI と揃える。** 今の CI（`code_check.yml` など）は Node 20 で、Node 20 は 2026-04 にサポートが終わっている。
> ビルド（GitHub Actions）と実行（VPS）を **Node 22 に揃える**変更を §6 のデプロイ PR に含める。

### 手順6: DNS（人）

`ezocivic.tech` のネームサーバーは今 Xserver にあるので、**Xserver の DNS レコード設定**で次を足す。
さくらのネームサーバーへ移すのは、今は利点が小さいのでやらない。

| ホスト名 | 種別 | 値 |
|---|---|---|
| `gikai` | A | VPS の IPv4 |
| `gikai-admin` | A | VPS の IPv4 |
| `db` | A | VPS の IPv4 |

- IPv6（AAAA）は、手順9 の nginx を IPv6 でも待ち受けるようにしてから足す。先に AAAA だけ足すと、IPv6 で来た利用者が繋がらない
- `ezocivic.tech`（傘のトップページ）と `studio.` は今回は作らない
- 反映の確認: `dig +short gikai.ezocivic.tech`（手元から）

### 手順7: Supabase（compose）を立ち上げる

§6 の PR（`infra/` の VPS 対応）がマージされてから行う。

**1. ファイルを置く**（手元のリポジトリ直下から）

```bash
ssh ezocivic-vps1 'sudo mkdir -p /srv/supabase && sudo chown ubuntu: /srv/supabase'
rsync -av --exclude volumes --exclude .env infra/ ezocivic-vps1:/srv/supabase/
```

**2. 秘密情報を作る**（手元で。作り方は検証手順 §3 手順0 と同じ。**ローカル検証で使った値は使い回さない**）

```bash
openssl rand -hex 32   # JWT_SECRET
openssl rand -hex 24   # POSTGRES_PASSWORD
# ANON_KEY / SERVICE_ROLE_KEY は検証手順 §3 手順0 の node のワンライナーで作る
```

作った4つはパスワードマネージャーに保存する。`JWT_SECRET` を失うと、全員のログインと ANON_KEY を作り直すことになる。

**3. `/srv/supabase/.env` を作る**（VPS で。`chmod 600`）

```bash
POSTGRES_PASSWORD=<生成した値>
JWT_SECRET=<生成した値>
ANON_KEY=<生成した値>
SERVICE_ROLE_KEY=<生成した値>

SUPABASE_PUBLIC_URL=https://db.ezocivic.tech
SITE_URL=https://gikai.ezocivic.tech
ADDITIONAL_REDIRECT_URLS=https://gikai-admin.ezocivic.tech/api/auth/callback

JWT_EXPIRY=3600
RATE_LIMIT_ANONYMOUS_USERS=30
GATEWAY_PORT=8000
DB_PORT=5433
```

**4. 起動**

```bash
cd /srv/supabase
docker compose up -d
docker compose ps                                  # db / auth / storage が healthy、rest / nginx が Up（healthcheck が無い）
curl -s 127.0.0.1:8000/auth/v1/health              # GoTrue
curl -s 127.0.0.1:8000/storage/v1/status           # storage-api
```

`restart: unless-stopped` なので、VPS を再起動しても docker と一緒に上がる。

### 手順8: マイグレーション・マスタ・管理者

DB は VPS の外に出さないので、**手元から SSH トンネル越しに**流す。VPS に supabase CLI やリポジトリは置かない。

```bash
# 手元（ターミナル1）
ssh -N -L 5433:127.0.0.1:5433 ezocivic-vps1

# 手元（ターミナル2、リポジトリ直下。develop の最新にしておく）
export PROD_DB_URL="postgresql://postgres:<POSTGRES_PASSWORD>@127.0.0.1:5433/postgres?sslmode=disable"
npx supabase migration up --include-all --db-url "$PROD_DB_URL"
psql "$PROD_DB_URL" -c "select id, public from storage.buckets"   # bill-thumbnails | t
psql "$PROD_DB_URL" -c "select name from prompts"                 # プロンプト3件（マイグレーションが入れる）
```

> 手元のローカル Supabase（`npx supabase start`）も 5433 を使っていないか確かめる。ぶつかる場合はトンネルの手元側のポートを変える。

**マスタ**: 会派・委員会（決算特別委員会の第一部・第二部を含む）・タグは、seed の `data.ts` の実データを使う。
`pnpm seed` は全件を消してから架空の議案まで入れるので**使わない**。マスタだけを入れるスクリプトを §6 で作り、それを流す。
定例会（`council_sessions`）は第3回定例会の分を、議案登録（T4b）の時に管理画面から作る。

**最初の管理者**: `supabase/seed.sql` の `admin@example.com` は使わない。GoTrue の admin API で作り、`admin_profiles` に行を足す。

```bash
# 手元から。SERVICE_ROLE_KEY は手順7で作った値。パスワードは長いものにする
curl -s -X POST https://db.ezocivic.tech/auth/v1/admin/users \
  -H "apikey: $SERVICE_ROLE_KEY" -H "Authorization: Bearer $SERVICE_ROLE_KEY" \
  -H "Content-Type: application/json" \
  -d '{"email":"<あなたのメールアドレス>","password":"<パスワード>","email_confirm":true}'

psql "$PROD_DB_URL" -c "insert into public.admin_profiles (user_id, role, display_name)
  select id, 'admin', '<表示名>' from auth.users where email = '<あなたのメールアドレス>'"
```

（`db.ezocivic.tech` は手順9 の後に使える。先にやる場合は `ssh -L 8000:127.0.0.1:8000` でトンネルを張り、`http://127.0.0.1:8000` に向ける）

2人目以降の管理者は管理画面から作る（`admin_profiles` も作られることを PR #12 で直してある）。

### 手順9: ホストの nginx と TLS

```bash
sudo apt install -y nginx certbot python3-certbot-nginx apache2-utils
```

**1. vhost を置く**: §6 の PR で `infra/host-nginx/` に作る3つのファイル
（`gikai.ezocivic.tech.conf` / `gikai-admin.ezocivic.tech.conf` / `db.ezocivic.tech.conf`）を
`/etc/nginx/sites-available/` に置き、`sites-enabled/` にリンクする。中身の要点:

- `gikai` → `127.0.0.1:3004`、`gikai-admin` → `127.0.0.1:3003`、`db` → `127.0.0.1:8000`
- 3つとも `proxy_set_header X-Real-IP $remote_addr;` と `X-Forwarded-For $remote_addr;`（利用者が送った値を**上書き**する）、
  `X-Forwarded-Proto $scheme;`
- web・admin は AI の応答をストリーミングで返すので `proxy_buffering off;`
- `db` は `client_max_body_size 50m;`（サムネイルのアップロード）
- **公開前は `gikai` と `gikai-admin` に Basic 認証をかける**（次の 2.）。`db` にはかけない

**2. 公開前の Basic 認証**

アプリにも Basic 認証（`BASIC_AUTH_USER` など）はあるが、**web のものは画面（HTML）にしか効かず、`/api/chat` などは素通しになる**
（`web/src/middleware.ts` の `_isHtmlRequest`）。公開前は nginx で**全パスに**かける。

```bash
sudo htpasswd -c /etc/nginx/htpasswd-gikai <ユーザー名>
```

```nginx
# gikai / gikai-admin の server ブロックに。公開する時（T1 の後）にこの2行を消す
auth_basic "EZO CIVIC (preview)";
auth_basic_user_file /etc/nginx/htpasswd-gikai;
```

admin から web の `/api/revalidate` を呼ぶ通信も nginx を通ると 401 になる。§6 で、admin から web へは
`http://127.0.0.1:3004` に直接送るようにする（`ADMIN` 側の環境変数で向け先を変える）。

`db` に Basic 認証をかけないのは、ブラウザの supabase-js が別オリジンの API に認証情報を付けないため。
`db` は RLS（ポリシー無し＝全拒否）で守られていて、anon キーでは何も読めない（検証結果 §3 #8）。

**3. 証明書**

```bash
sudo nginx -t && sudo systemctl reload nginx
sudo certbot --nginx -d gikai.ezocivic.tech -d gikai-admin.ezocivic.tech -d db.ezocivic.tech \
  -m <連絡用メールアドレス> --agree-tos --redirect
sudo certbot renew --dry-run      # 自動更新（systemd timer）が通るか
```

### 手順10: web・admin を置いて起動する

**1. 実行時の環境変数**: `/srv/gikai/web.env` と `/srv/gikai/admin.env`（`chmod 600`）

| 変数 | web | admin | 値 |
|---|---|---|---|
| `SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_URL` | ○ | ○ | `https://db.ezocivic.tech` |
| `SUPABASE_SERVICE_ROLE_KEY` | ○ | ○ | 手順7 の値 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ○ | ○ | 手順7 の値 |
| `NEXT_PUBLIC_WEB_URL` | ○ | ○ | `https://gikai.ezocivic.tech` |
| `NEXT_PUBLIC_APP_URL` | ○ | ○ | `https://gikai-admin.ezocivic.tech` |
| `ADMIN_URL` | ○ | | `https://gikai-admin.ezocivic.tech`（プレビュー画面から管理画面へ戻るリンク。未設定だと `http://localhost:3001` になる） |
| `REVALIDATE_SECRET` | ○ | ○ | `openssl rand -hex 32`。両方に同じ値 |
| `OPENAI_API_KEY` | ○ | ○ | OpenAI の本番用キー |
| `AI_GLOBAL_DAILY_COST_LIMIT_USD` | ○ | | 公開前は `1`。公開時に `2` から始める（方針 §5） |
| `CHAT_DAILY_COST_LIMIT_USD` / `INTERVIEW_DAILY_COST_LIMIT_USD` | ○ | | 方針 §5 の目安で決める |
| `PORT` / `HOSTNAME` | ○ | ○ | web `3004` / admin `3003`、どちらも `127.0.0.1` |
| `NODE_ENV` | ○ | ○ | `production` |
| `ADMIN_ENABLE_CLAUDE_CLI` | | ✕ | **設定しない**。Claude CLI を使う管理画面の機能はローカル専用（`.env.example` では `true`） |

どの変数をどちらのアプリが読むかは、§6 のデプロイ PR で `.env.example` と突き合わせて確定する。

**`NEXT_PUBLIC_*` はビルド時に JS に埋め込まれる**（検証手順 §3 手順4）。上の表の `NEXT_PUBLIC_*` は、
**GitHub Actions のビルドにも同じ値を渡す**（GitHub の Variables に登録する。anon キーは公開して構わない値）。
サービスロールキーなどの秘密は VPS の env ファイルにだけ置き、GitHub には置かない。

**2. 最初のデプロイ**: §6 のデプロイ用ワークフローを `workflow_dispatch` で手動実行する。中身は次のとおり。

1. `pnpm install --frozen-lockfile` → `pnpm --filter web build` / `pnpm --filter admin build`（本番の `NEXT_PUBLIC_*` を渡す）
2. `.next/standalone` に `.next/static` と `public` をコピー（検証結果 §4-6）
3. `rsync --delete` で `/srv/gikai/web/` と `/srv/gikai/admin/` に送る（デプロイ用の SSH 鍵を使う）
4. `sudo systemctl restart gikai-web gikai-admin`

デプロイ用の SSH 鍵は専用に作り、VPS の `authorized_keys` に足して、秘密鍵を GitHub Secrets に登録する（人）。
`systemctl restart` だけを `sudo` で許す設定（`/etc/sudoers.d/`）も §6 の PR に含める。

**3. systemd unit**: §6 の PR で作る `infra/systemd/gikai-web.service` / `gikai-admin.service` を
`/etc/systemd/system/` に置く。要点:

```ini
[Service]
User=ubuntu
WorkingDirectory=/srv/gikai/web/web
EnvironmentFile=/srv/gikai/web.env
ExecStart=/usr/bin/node server.js
Restart=on-failure
# 2GB の VPS で1つのアプリがメモリを食い尽くさないように
MemoryMax=600M
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now gikai-web gikai-admin
journalctl -u gikai-web -f
```

### 手順11: 定期処理（cron）

§6 の PR で `infra/scripts/` に作るスクリプトを置き、`/etc/cron.d/gikai` から呼ぶ。

| 時刻 | 処理 | 中身 |
|---|---|---|
| 毎日 3:00 | バックアップ | 検証結果 §4-5 の3点（`migrations.txt` / `data.sql` / `storage.tar`）を `/srv/backups/<日付>/` に取る。14日より古いものを消す |
| 毎日 3:30 | 会話ログの削除 | `docker compose exec -T db psql -U postgres -c "select public.delete_expired_chat_logs();"`（90日で消す。方針 §5 ④） |

- **バックアップを VPS の中にだけ置いても、VPS が壊れたら一緒に失う。** 外への持ち出し先は §7 で決める
- バックアップには市民のインタビュー回答・利用者のメールアドレスとパスワードハッシュが入る。`/srv/backups` は `chmod 700`
- **会話ログを消す cron を仕掛けてから、プライバシーポリシーに「90日で自動的に削除」と書く**（文案 §3）

### 手順12: 動作確認

ローカル検証（検証手順 §4）のチェックリストを、本番のドメインでもう一度行う。加えて、VPS でしか確かめられないもの:

| # | 確かめること | やり方 |
|---|---|---|
| 1 | web・admin・db に https で入れ、http は https に飛ぶ | ブラウザ、`curl -I http://gikai.ezocivic.tech` |
| 2 | Basic 認証が全パスに効いている | `curl -s -o /dev/null -w "%{http_code}" -X POST https://gikai.ezocivic.tech/api/chat` が 401 |
| 3 | 匿名ログインの上限が **IP ごと**に数えられる（2段の nginx で IP が引き継がれている） | 検証手順 §4 #9 を、手元の回線とスマホの回線（別 IP）から。上限を一時的に 3 に下げて行う |
| 4 | admin でログイン → 議案を編集・公開切替 → web に反映 | 検証手順 §4 #4・#7 |
| 5 | サムネイルをアップロードし、web に表示される | `db.ezocivic.tech` 経由。`images.remotePatterns` の確認を兼ねる |
| 6 | チャットとインタビューを1往復ずつ | `chat_usage_events` に記録される |
| 7 | VPS の管理画面に Claude CLI の機能が出ない | admin の議案一覧・`/ai-collection`（§6 の修正の確認） |
| 8 | VPS を再起動しても全部上がる | `sudo reboot` の後に #1 |
| 9 | バックアップから戻せる | 検証結果 §4-5 の復元を、手元の compose に対して行う（VPS には戻さない） |
| 10 | メモリ | `free -h`、`docker stats --no-stream`、`systemctl status gikai-web` の Memory |
| 11 | 数日後もキャッシュの無効化が即座に効く | 検証結果 §4-8。立ち上げて数日後に #4 をもう一度 |

---

## 5. 公開する時（T1 の判断の後）

- nginx の Basic 認証の2行を消して reload
- AI コスト上限を公開時の値にする（`AI_GLOBAL_DAILY_COST_LIMIT_USD=2` から）。**OpenAI 側のハードリミットが「超えたら拒否」になっているかを先に確かめる**（人。タスク分割の持ち越し）
- プライバシーポリシーと FAQ に文案 §3 の3点（国内のデータセンター、AI チャットは90日で削除、サーバーログの保存期間）を足し、制定日を入れる
- 公開 URL で OGP をデバッガーで確認し、スマホの「ホーム画面に追加」を試す
- `.github/workflows/deploy_web.yml`（Vercel Deploy Hook）を消す。Vercel のプロジェクトも止める

---

## 6. 先に PR で用意するもの（Claude）

手順7 以降の前提。独立しているので、分けて PR にしてよい。

| # | 内容 | 使う手順 |
|---|---|---|
| 1 | **compose の nginx で利用者の IP を引き継ぐ**: `infra/nginx/supabase.conf` に `set_real_ip_from`（docker のネットワーク）と `real_ip_header X-Real-IP` を足し、`X-Forwarded-Proto` もホストの値を引き継ぐ。**これが無いと全員が同じ IP に見え、サイト全体で匿名ログインが1時間30件で止まる**（検証結果 §4-2） | 手順7 |
| 2 | **ホストの nginx の vhost**（`infra/host-nginx/`）。Basic 認証、ストリーミング、`X-Real-IP` の上書き | 手順9 |
| 3 | **admin → web の revalidate を内部で送る**: 公開前の Basic 認証で 401 にならないよう、`http://127.0.0.1:3004` に直接送れるようにする | 手順9・10 |
| 4 | **admin の「ローカルかどうか」の判定を直す**: `process.env.VERCEL` が無いとローカル扱いになり、VPS の管理画面に Claude CLI の機能が出る（`admin/src/features/bills/server/components/bill-list/bill-list.tsx:46`、`admin/src/app/(protected)/ai-collection/page.tsx:8`）。専用の環境変数に置き換える | 手順10 |
| 5 | **`images.remotePatterns` に `db.ezocivic.tech` を足す**（web / admin の `next.config.ts`） | 手順10 |
| 6 | **デプロイ**: GitHub Actions のワークフロー（ビルド → rsync → restart）、systemd unit（`infra/systemd/`）、sudoers、CI と VPS の Node を 22 に揃える | 手順10 |
| 7 | **マスタだけを入れるスクリプト**（`packages/seed/`。会派・委員会・タグ。既にあれば飛ばす） | 手順8 |
| 8 | **cron とバックアップのスクリプト**（`infra/scripts/`） | 手順11 |

---

## 7. 決めること・直すこと

### 決めること

| 論点 | 案 |
|---|---|
| バックアップの持ち出し先 | (a) 手元の WSL から毎日 `rsync` で取りに行く（無料。手元の PC が点いている時しか取れない）、(b) さくらのオブジェクトストレージ（月額が掛かる、国内のまま）。**まずは (a)**、公開後に (b) を検討 |
| サーバーログの保存期間と閲覧者 | journald の保存期間を決める（例: 30日、`SystemMaxRetentionSec=30d`）。LLM のエラー時に市民の発言がログに出るため。閲覧できるのは VPS に入れる人（今はあなただけ）。決めたらプライバシーポリシー第8条に足す |
| Storage を残すか | 今は storage-api を載せる（検証済み）。nginx の静的配信に替えると約 116MiB 減る（方針 §9-1）。メモリが苦しくなったら検討 |
| 管理者パスワードの最低文字数 | 今は 6（`GOTRUE_PASSWORD_MIN_LENGTH`）。議員に開放する前に見直す（T10） |
| IPv6 | 公開前に AAAA と nginx の `listen [::]:443` を足すか。足さなくても IPv4 で繋がる |

### ドキュメントの直し（この PR で行う）

- 方針ドキュメント §5 の作業項目から「本番データでの予行演習」「本番（Supabase Cloud）の Confirm email 確認」を消す（本番が存在しないため）
- タスク分割の T7 の「やること」から「本番データでの予行演習」を消し、「注意: 本番 dump には〜」を
  「VPS のバックアップには市民の回答が入る」に直す。読むものに本書を足す

### 後回しにするもの

- SMTP（`noreply@ezocivic.tech`）— T10 で。足す時にメールでのサインアップを塞ぐ手段も決める（検証結果 §4-3）
- `.env.production` と `db-access` スキルの接続先を VPS に切り替える — 手順8 が終わってから
- `ezocivic.tech`（傘のトップページ）と `studio.ezocivic.tech`
