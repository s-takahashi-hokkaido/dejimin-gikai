# プロジェクト方針とやりたいこと整理

作成日: 2026-09-12
最終更新: 2026-09-23（リブランド PR #13・#14 のマージを反映: 表示名・ロゴ・パッケージ名の完了分にチェック / 残った operator 表記・README・帰属表記を残作業として明記）

本ドキュメントは、本プロジェクトの位置づけ（思想）を明文化し、そこから降りてくる
「やりたいこと」を棚卸しして優先順位を付けるためのもの。
個別の実装計画はここでは書かず、塊ごとに別途設計文書を起こす。

---

## 1. 背景と位置づけ

### 1-1. これは何か

**高橋崇一郎 個人の CivicTech 活動**である。チームみらいの公式プロジェクトではない。
upstream（福岡市版／国政版の OSS）を土台にしているのは、自分の目的との親和性が
たまたま高かったからにすぎない。

- **プロダクト哲学**: 多様な民意を、議会に届け、反映する
- **立ち位置**: 意見を選別・代弁せず「届く場」を提供する**プラットフォーマー**

### 1-2. なぜ「脱・チームみらい色」なのか

党派色は、本プロジェクトの目的に対して**実務上の足かせ**になる。

- 議員・市長側に管理画面を使ってほしい（会派見解の記入、議案データの妥当性検証）
- 市民側からは党派を問わず声を集めたい

特定政党の色が付いていると、この両方が成立しない。
思想的な理由というより、**プロダクトが機能するための必要条件**として脱色する。

### 1-3. なぜ日系ベンダなのか

公共に近い領域を扱う以上、なるべく日系ベンダで動かしたいという個人の思想。
さくらインターネットは石狩にデータセンターを持ち、北海道に投資している事業者であり、
「札幌市議会版」という本プロジェクトの地域性とも一致する。

コスト判断ではなく、**何に金を払うかで立場を表明する**という選択。
これ自体が、同じ価値観の人と繋がるためのフックにもなる。

> **要ファクトチェック**: 発表・README に書く際は以下を裏取りすること。
> - 石狩DCの所在・規模、再生可能エネルギー利用の取り組み
> - ISMAP 登録は「さくらのクラウド」であって「さくらのVPS」とは別サービスの可能性が高い。混同しないこと

### 1-4. これを皮切りにしたい

本プロジェクトは単体の完成が目的ではなく、**これをきっかけに人と交流を持ち、
次に作るものを決めていく**ための第一歩。したがって:

- 完成度より**公開の早さ**を優先する
- 構成そのもの（AGPL・国内自前運用）が発信材料になる
- インフラは**このプロジェクト専用に固めず、共通基盤として組む**

---

## 2. 判断の原則（外部依存の線引き）

「日系ベンダで」を全てに適用すると GitHub も Claude Code も使えず破綻する。
以下の原則で線を引く。

> **市民から預かるもの**（インタビュー回答・意見・アクセスログ）は日本国内・日系事業者の管理下に置く。
> **作る側の道具**（GitHub、Claude Code、LLM API）は外部でよい。

これは実際の公共調達でも使われる線引き（データの所在 vs 開発ツール）であり、説明が通る。

| 依存 | 扱うもの | 判定 |
|---|---|---|
| ホスティング（Vercel） | 市民のリクエストが通る実行環境 | **移行**（さくらVPS） |
| Supabase（DB/Auth/Storage） | インタビュー回答＝市民の生の意見 | **移行**（VPS上にセルフホスト） |
| Google Analytics | 訪問者の行動ログ | **外す**（2026-09-21 決定） |
| Langfuse Cloud | プロンプトとLLM呼び出しログ | **外す**（2026-09-21 決定。§5 ④） |
| **LLM API（OpenAI等）** | 推論のために意見が渡る | **例外。外部に出ることを明示する** |
| GitHub | ソースコード（公開物） | 外部でよい |
| ドメインレジストラ | — | **Xserverドメイン**（日系）。`ezocivic.tech` 取得済み |

LLM だけが原則からはみ出る。ここは**はみ出していると自分で明示する**（プライバシーポリシーに記載）。
他を固めているからこそ、この1点の説明がしやすくなる。

---

## 3. やりたいこと（4つの塊）

| # | 塊 | 目的 | 優先度 |
|---|---|---|---|
| A | リブランド（デジ民議会） | 党派色を外し、議員・市民双方が使える中立性を得る | 高 |
| B | さくらVPSへのインフラ移行 | 思想の実装。国内自前運用 | 高 |
| C | 管理画面のロール権限 | 議員・出馬者に開くための前提 | 高 |
| D | AI変動費のガード | 公開時の事故防止。**固定費より読めないリスク** | 最高 |

優先度は「公開までに必須か」で付けた。D は実装量が小さいわりにリスクが大きいので最優先。

---

## 4. A. リブランド

> **2026-09-21 改訂**: プロダクト名を「オープン議会（OpenGikai）」から「**デジ民議会**」に変更した。

### 決定事項

- **活動体（傘）名: EZO CIVIC** — アルファベット表記のまま使う（カタカナ併記はしない）
- **傘ドメイン: `ezocivic.tech`** — Xserverドメインで取得済み
- **プロダクト名: デジ民議会**（「デジタル民主主義」の略）
- **プロダクトURL: `gikai.ezocivic.tech`**。プロダクト名の独自ドメインは当面取得しない

### 名前の選定理由

「デジタル民主主義」の略。**やりたいこと（§1-1「多様な民意を、議会に届け、反映する」）が
名前からそのまま伝わる**ことを取った。

> **旧案「オープン議会」の選定理由（記録）**: 「議員が使っても政治的に安全と感じる中立性」を最優先制約とし、
> 「オープンガバメント＝情報公開」という表立って反対しづらい枠組みを選んでいた。
> 英語名（OpenGikai）がそのまま使える点も利点としていた。

### 「デジタル民主主義」という語について

旧案で最優先にしていた「**名前が持つ政治的な含みは後から消せない**」という観点は、デジ民議会でも生きている。
「デジタル民主主義」は一般的な語だが、国内ではチームみらい（国政版みらい議会の開発元）の主張と結び付けて語られることも多い。
§1-2 の「脱・チームみらい色」と逆向きの連想を招く可能性はゼロではない。

「EZO」と同じく、**知ったうえで選んでいる**状態にしておき、聞かれたら答えられるようにしておく
（特定の政党の標語ではなく一般名詞として使っていること、運営は個人で政党とは無関係であること）。

### `.tech` を選んだ理由

- 安価
- CivicTech と意味が合う
- **自治体公式と誤認されない**（`.jp` / `lg.jp` より明確に民間・非公式に見える）

非公式であることの明示が必要な本案件では、`.jp` より適している。
レジストラは日系の Xserverドメインにしたので、事業者としての一貫性も保てている。

### なぜ傘ドメインを1つにするか

ドメインは1個で足りる。サブドメインは無料・無制限で、nginx も Let's Encrypt も
複数ホスト名を同時に扱えるため、**技術的な制約はゼロ**。純粋にブランディングとコストの判断。

プロダクト名のドメイン（旧案では `opengikai.tech`）は「議会もの」という**プロダクト名**であり、
将来まったく別のものを作った時に傘にできない。そこで階層を2つに分ける。

| 階層 | 役割 | 名前 |
|---|---|---|
| **活動体（傘）** | CivicTech 活動そのもの。ドメイン・メール・名乗りの基点 | **EZO CIVIC** (`ezocivic.tech`) |
| **プロダクト** | 個別のサービス | **デジ民議会** (`gikai.ezocivic.tech`) |

傘を1つにすればプロジェクトが何個増えても年額が変わらない。
「作るものは未定」という状況に強く、「お金をあまりかけたくない」方針とも整合する。

副次的な効果:

- **ドメインメールが持てる**（`noreply@ezocivic.tech`）。C のロール権限で SMTP が必須になるため、
  議員宛のメールが Gmail から飛ぶのとドメインメールから飛ぶのとでは信頼性も到達率も違う
- 内部用サブドメイン（Studio・監視）を1つのドメイン配下にまとめられる
- 傘の名前＝活動体の名前＝議員に自己紹介する時の名乗りになる

> `ezo.tech` は Afternic（ドメイン売買サイト）に駐車済み＝転売目的で保有されているため避ける。
> ハイフン無しの `ezocivic` を選んだ理由は、**傘ドメインがメールアドレスになるから**。
> 口頭で「エゾ、ハイフン、シビック」と説明するコストを毎回払いたくない。

### サブドメイン設計

**すべて1階層に揃える。** 2階層にすると証明書の扱いが面倒になる
（ワイルドカード証明書は1階層しかカバーしない）。

```
ezocivic.tech              → 活動体のトップページ
gikai.ezocivic.tech        → デジ民議会（web / 公開）
gikai-admin.ezocivic.tech  → 管理画面（議員・出馬者）
db.ezocivic.tech           → Supabase API（PostgREST / GoTrue）
studio.ezocivic.tech       → Supabase Studio（IP制限 or 非公開）
```

**ワイルドカード証明書は不要。** certbot の HTTP-01 認証でホスト名ごとに取れば
DNS API 連携が要らず、増えたら足すだけで済む。

### 名乗り方

| 場所 | 表記 |
|---|---|
| サイトタイトル | デジ民議会 札幌市版 |
| フッター | 運営: EZO CIVIC（高橋崇一郎） |
| 利用規約の operator（`site.config.ts`） | EZO CIVIC / 代表 高橋崇一郎 |
| メールの From | `noreply@ezocivic.tech` |
| GitHub Organization | `ezocivic`（作るなら） |

責任の所在の観点から、**団体名と個人名は併記する**。個人名は隠さない。

### マルチ自治体への逃げ道

将来の道議会・他市町村対応は**設計に織り込まない**（スピード優先）。
多自治体化したくなった時点でプロダクト名の独自ドメインを取得し、そちらへ引っ越す
（デジ民議会のローマ字・英語表記とあわせて未決。§9）。

```
（現在）  gikai.ezocivic.tech
（将来）  sapporo.<プロダクト名ドメイン> / hokkaido.<プロダクト名ドメイン>
```

サブドメイン → 独自ドメインへの引っ越しはリダイレクトで済む。逆は面倒なので、この順序なら問題ない。
サイト名は `web/src/config/site.config.ts` に既にある構造で「デジ民議会 札幌市版」として出せる。

### 「EZO」という語について（採用済み・記録として）

北海道では「エゾ」は極めて一般的な語（エゾシカ、エゾリス、えぞ財団など）で、
多くの人にとって「北海道らしさ」を表す親しみのある言葉。
行政区分である「北海道」より文化的な広がりがあり、名前として良い選択。

一方で、蝦夷／蝦夷地は明治の開拓期に「北海道」へ改称された、**植民の歴史を背負った語**でもある。
議会を扱う以上アイヌ施策関連の議案は出てくるため、その文脈で名前について指摘を受ける可能性はゼロではない。

一般的な語感の方が優勢と判断して採用したが、**知ったうえで選んでいる**状態にしておき、
聞かれたら答えられるようにしておく。

### 作業項目

- [x] `ezocivic.tech` の取得 — **Xserverドメインで取得済み**（2026-09-21 時点）
  - **さくらのドメインは `.tech` を扱っていない**（2026-09-17 価格一覧で確認）ため、国内の他社から選んだ
  - `.tech` のような新gTLDは初年度が安く更新が高いのが典型パターン。更新前に価格を確認する
  - DNS はさくらに寄せられる。さくらのVPS契約者はネームサーバーサービス（`ns1.dns.ne.jp` / `ns2.dns.ne.jp`）を
    無料枠内で使え、他社で取得したドメインのゾーンも編集できる
- [ ] 先行サービス・商標の衝突調査（EZO CIVIC / デジ民議会 の両方）
- [ ] DNS レコード設定（上のサブドメイン設計）
- [ ] `noreply@ezocivic.tech` のメール設定（C の SMTP 要件と同時にやる）
- [x] `site.config.ts`（web / admin）の**サイト名・説明文**の書き換え（PR #13）
- [ ] `site.config.ts` の **operator 表記**を「EZO CIVIC / 代表 高橋崇一郎」に直す
      — 現状は `operator.name = "s.takahashi"`（`web/src/config/site.config.ts:52`）。
      §4「名乗り方」の表（団体名と個人名を併記する）に合わせる
- [ ] `siteConfig.externalLinks` の `donation` / `teamAbout` の扱いを決める
      — 現状 `team-mir.ai` を指したまま（`web/src/config/site.config.ts:39-40`）。
      `showTeamMiraiSection: false` なので画面には出ていないが、設定としては旧運営元に向いている
- [x] ロゴ・favicon・OGP の作り直し（PR #9 で OGP を PNG 化・admin ファビコン、PR #13 でロゴ・PWA アイコン・favicon）
- [ ] **README の書き換え** — 現状は「チームみらいの fork、非公式」という説明のまま（`README.md:7-10`）。
      人に見せて仲間を集めるなら、「このプロジェクトは何で、なぜ存在するか」を語る場所にする。
      PR #14 で旧名称のローカル環境からの移行手順は足したが、冒頭の位置づけは手つかず
- [x] コード内の「みらい議会」表記の洗い出しと置換（PR #13）
      — `web` / `admin` のソースに残る「みらい議会」「チームみらい」は派生元への帰属表記のみ
      （`about.tsx` / `faq/page.tsx` / `footer.tsx` など）。`docs/` 配下の過去の設計書は当時の記録として変更しない
- [ ] 「チームみらい」表記のうち**消す／見直す**と決めた分を反映する
      — 方針（D7）は
      [名称変更_影響箇所調査とチェックリスト](20260923_1153_デジ民議会への名称変更_影響箇所調査とチェックリスト.md) で確定済みで、
      「残す」（派生元の説明）と「変える」（自サービスの名乗り）は PR #13 で反映した。
      未着手なのは「自サービスがチームみらいの活動の一部であるかのような文言」の整理:
      `footer.tsx:105` の `© 2025 Team Mirai`、`hero.tsx:27` の `powered by Team Mirai & AI`、
      `team-mirai.tsx` / `social-links.ts` / `team-mirai-typography.svg` の去就、`CLA.md` の扱い。
      いずれも `showTeamMiraiSection: false` で現在は非表示だが、設定をオンにすると出る
- [ ] about・FAQ・footer の帰属表記が「デジ民議会」と並んで文として自然か目視で確認する
- [x] パッケージ名を `@mirai-gikai/*` → `@dejimin-gikai/*` に改名（PR #14）。
      あわせて GitHub リポジトリを `dejimin-gikai` に、Supabase ローカルの `project_id` を `dejimin-gikai` に変更。
      CSS トークン `--color-mirai-*` と DB の `mirai_stances` は内部識別子として据え置き

### AGPL-3.0 の制約

本リポジトリは **AGPL-3.0**。改名・リブランドは自由だが以下を守る。

- ライセンスは AGPL-3.0 のまま、元の著作権表示を残す
- **AGPL §13**: ネットワーク越しに使わせる場合、ソースコードの入手経路を利用者に提示する義務がある。
  フッターの GitHub リンクがその役割を果たすため、**リブランド時に消さないこと**

むしろ「AGPL で全部公開、国内のサーバーで自分で運用」という一貫した説明ができる点で追い風。

---

## 5. B. さくらVPSへのインフラ移行

### 決定事項

- **さくらVPS 2GB プラン 1台に全部載せる**（段階移行はしない）
- **VPS 上でビルドしない**
- **Supabase は最小構成でセルフホスト**
- **admin も VPS に載せる**（議員が使うため）＋ **ローカルにも同じ admin を立てる**
- 土台は「2個目のプロジェクトを足すのが簡単」を基準に組む

### なぜ段階移行しないか

当初「Phase 1: ホスティングのみ → Phase 2: DB移行」を検討したが撤回した。

1. 札幌市限定・低トラフィックなので、リスク分散のために段階を刻む必要が薄い
2. 半分だけ移行した状態は「日系で動かしている」という**説明が中途半端**になる
3. 未公開なので失敗しても誰も困らない。今が一番大胆に動ける

ただし**ローカルで docker compose を通して動作確認してから VPS に持っていく**。
VPS 上でいきなり試行錯誤しない。

### サイジング根拠

| 内訳 | 概算 |
|---|---|
| OS | ~200MB |
| Postgres（`shared_buffers` を絞る） | ~300MB |
| PostgREST + GoTrue（Goバイナリ） | ~80MB |
| storage-api | ~100MB |
| Next.js web（standalone） | ~250MB |
| Next.js admin（standalone） | ~250MB |
| nginx | ~20MB |
| **合計** | **~1.2GB** |

2GB で収まる。1GB は余裕が無さすぎるため非推奨。月1,700円前後（**要確認**）＋ドメイン年数千円。

> VPS は月単位契約で後からプランを上げられるはず。**プラン変更時にデータが引き継がれるか、
> 再インストールが必要かは契約前に確認すること**。

### 移行対象の棚卸し

#### ① ホスティング = Vercel → 移行難易度: 低

| 箇所 | 内容 |
|---|---|
| `.github/workflows/deploy_web.yml` | Vercel Deploy Hook を curl で叩くだけ |
| `web/src/app/(main)/layout.tsx:2,18` | `@vercel/speed-insights` |
| `web/next.config.ts` / `admin/next.config.ts` | `images.remotePatterns`（Supabase Storage 向け） |

**Edge Runtime を一切使っていない**（`export const runtime = "edge"` がゼロ）ため、
`next build && next start` を systemd で起こし nginx を前段に置けば動く。
`unstable_cache` は self-host の `next start` でもファイルシステムキャッシュとして機能する。
middleware も Node ランタイムで動作する。

注意点は `next/image` の最適化がサーバー CPU を食うこと（Vercel が肩代わりしていた部分）。
画像が少ないサイトなので `images.unoptimized` か nginx 配信でも実害は小さい。

#### ② DB / Auth / Storage = Supabase → セルフホスト

依存は3層。

1. **DB アクセス（PostgREST）** — 全 `repositories/` が `supabase-js` の `.from().select()` を使用。
   素の Postgres + Drizzle/Kysely に置き換えると全リポジトリ層の書き換えになる → **やらない**
2. **Auth（GoTrue）** — admin ログイン ＋ web の匿名ログイン
   （`web/src/features/chat/client/hooks/use-anonymous-supabase-user.ts:31` の `signInAnonymously`、
   チャットのレート制限／コスト管理に使用）
3. **Storage** — `bill-thumbnails` バケット
   （`admin/src/features/bills-edit/client/lib/thumbnail-storage.ts`）

Supabase は Apache-2.0 でセルフホスト可。**アプリ側の変更はほぼ環境変数の差し替えのみ**で済む。

立てるコンテナの取捨選択:

| コンテナ | 判断 |
|---|---|
| postgres / postgrest / gotrue | **必須** |
| kong | **不要**（nginx で代替） |
| studio | **不要**（SSHトンネルでローカルから） |
| realtime | **既に無効**（`supabase/config.toml:73`） |
| storage-api | 削れる可能性あり。用途が `bill-thumbnails` だけなので nginx 静的配信＋Server Action でのファイル保存に変えれば1つ減る。実装コストとのトレードオフ |
| imgproxy / analytics / vector | 不要 |

その他の対応:
- **匿名サインインを GoTrue 設定で有効化**する必要がある
- マイグレーションは `supabase` CLI の `--db-url` 指定で流せる

#### ③ AI = Vercel AI Gateway → 移行難易度: 低〜中

`gateway(AI_MODELS.gpt4o)` 形式で使用
（`web/src/features/chat/server/services/handle-chat-request.ts:101`、
`web/src/features/interview-session/server/services/handle-interview-chat-request.ts:219-220`）。

**AI Gateway は単なる HTTP API なので、VPS からでもそのまま動く**（機能的には壊れない）。
それでも切るなら:

- `packages/shared/src/ai/models.ts` のモデル ID が `"openai/gpt-4o"` という Gateway 形式なので、
  `openai("gpt-4o")` / `anthropic(...)` に振り分ける **resolver 関数を1つ作る**
- 既に DI 構造（`deps?.model ?? gateway(...)`）があるので差し込み口はある
- `web/src/lib/ai/calculate-ai-cost.ts` の価格テーブルが Gateway 形式キー前提のため、
  **キー体系は維持したままモデル解決だけ差し替える**

#### ④ Langfuse — 現状「落とせない」実装になっている

`web/src/lib/prompt/index.ts:23-31` の `createPromptProvider()` は、
Langfuse クライアントの初期化に失敗すると **throw する**。フォールバックが無い。

ただし同ファイルのコメントに「モック実装の追加手順」が明記されており、
`PromptProvider` インターフェースで抽象化済み。**ファイルベースの実装を1つ足すだけ**で切れる。

Langfuse のセルフホストは近年 ClickHouse + Redis + MinIO まで要るため、
**小さい VPS では論外**。選択肢は「クラウド無料枠に留める」か「実装して切る」の二択。

**決定（2026-09-21）: 切る。** Langfuse の用途は2つあり、両方を外す。

1. **プロンプト管理** — `LangfusePromptProvider` が Langfuse 上のプロンプトを取得している。
   プロンプト本文をリポジトリのファイルに移し、ファイルベースの `PromptProvider` に差し替える
2. **LLM 呼び出しのトレース** — `web/src/lib/telemetry/register.ts` / `admin/src/lib/telemetry/register.ts` が
   `langfuse-vercel` の exporter で送信している（認証情報が無ければ警告を出して無効化される作り）

`interview_sessions.langfuse_session_id` カラムが残るので、消すか放置するかも合わせて決める。

#### ⑤ Claude CLI 依存 — admin をローカルにも立てて回避

`admin/src/features/ai-collection/server/utils/execute-claude.ts:63` と
`admin/src/features/bills/server/actions/run-auto-feature.ts:34` が
`spawn("claude", ...)` でローカルの Claude Code CLI を叩いている。

> 補足: AIインタビュー機能は Claude ではなく Vercel AI Gateway 経由の GPT-5.2 系。
> Claude CLI 依存はこの2機能のみ。

**対応**: 同じ admin アプリを VPS とローカルの両方で起動し、DB を共有する。

- **VPS の admin** → 議員・出馬者・admin が使う。通常の編集作業
- **ローカルの admin** → admin 専用。Claude CLI 依存機能はここから実行
  （`pnpm dev:admin:prod-db` が既に存在）

Claude CLI 依存機能は admin ロール限定にすれば議員の画面には出ない。
VPS に Claude CLI を入れる必要もない。

### 共通基盤としての組み方

「作るものが未定」だからこそ、**土台をこのプロジェクト専用に固めない**。

- nginx は「vhost を1個足せばプロジェクトが増える」形にする
- **Postgres は共有インスタンス、プロジェクトごとに database を分ける**（メモリ節約が一番効く）
- デプロイは「GitHub Actions でビルド → rsync → systemd restart」というパターンを1つ作って使い回す
- 技術選択は徹底的に退屈にする。docker compose 1枚 + systemd + nginx。
  ひとりで未定の未来を運用するなら Kubernetes も凝った CD も敵
- バックアップは `pg_dump` を cron で回して手元か安いオブジェクトストレージへ

**2個目のプロジェクトを載せる作業が「vhost + systemd unit + database を足すだけ」になっていれば成功。**

### 作業項目

- [ ] さくらVPS 契約（プラン変更可否を事前確認）
- [x] `next.config.ts` に `output: "standalone"` を追加（web / admin）
- [x] ローカルで Supabase 最小構成の docker compose を作成・動作確認（`infra/`）
      — 手順: [ローカル docker compose 検証手順](20260922_1716_ローカルdocker-compose検証手順.md)、
      結果: [ローカル docker compose 検証結果](20260922_2100_ローカルdocker-compose検証結果.md)。
      本番データでの予行演習（手順6）は未実施
- [ ] nginx + Let's Encrypt の共通リバースプロキシ構成
- [ ] systemd unit のテンプレート化
- [ ] GitHub Actions のデプロイを rsync + systemd restart に差し替え
      （`deploy_web.yml` の Vercel Deploy Hook を廃止）
- [ ] `@vercel/speed-insights` の削除
- [ ] `images.remotePatterns` を新ドメインに合わせて修正
- [ ] standalone の起動スクリプト（`.next/static` と `public` のコピー＋ `server.js` 起動）をデプロイと合わせて作る
      （検証結果 §4-6）
- [x] GoTrue の匿名サインイン有効化（`infra/compose.yml` の `GOTRUE_EXTERNAL_ANONYMOUS_USERS_ENABLED`）
- [ ] 本番データでの予行演習（検証手順の手順6）— 本番 dump は市民のインタビュー回答を含むので取り扱い注意
- [ ] チャット・インタビューの往復を compose 構成で確認（AI Gateway のキーが要る。Langfuse 撤去前は Langfuse のキーも）
- [ ] 本番（Supabase Cloud）の「Confirm email」設定の確認（検証結果 §4-3）
- [ ] VPS 上での再確認 — nginx 構成に応じた匿名ログインのレート制限（`GOTRUE_RATE_LIMIT_HEADER`、検証結果 §4-2）、
      長く動かした web でのキャッシュ無効化（検証結果 §4-8）
- [ ] SMTP 設定（後述の C でも必要）— 足す時にメールでのサインアップを塞ぐ手段と送信レート制限も決める（検証結果 §4-3）
- [ ] Storage をセルフホストするか nginx 静的配信にするか判断（compose では storage-api を載せて検証済み）
- [ ] Langfuse を外す（§5 ④）— プロンプト本文をファイルへ移してファイルベースの `PromptProvider` に切り替え、
      テレメトリ（web / admin の `lib/telemetry/register.ts`）と Langfuse 関連のパッケージ・環境変数を削除する
- [ ] **プロバイダ側（OpenAI / AI Gateway）に月額ハードリミットを設定する**
      — アプリ側のガードは fail-open（判定が失敗したら続行）なので、コードの正しさに依存しない
      最後の防波堤が要る。請求を本当に止めるのはこちら
- [ ] AIコスト上限の本番値を決めてサーバーの環境変数に設定する
      （`AI_GLOBAL_DAILY_COST_LIMIT_USD` / `CHAT_DAILY_COST_LIMIT_USD` / `INTERVIEW_DAILY_COST_LIMIT_USD`）
      — 目安: 1セッション $0.05〜0.07 程度（概算）。まず全体 $2/日 から始め、
      公開後に `chat_usage_events` の実績で調整する
- [ ] `pg_dump` バックアップの cron 設定
- [ ] GA を外す — `web/src/app/(main)/layout.tsx` の `GoogleAnalytics` と
      `NEXT_PUBLIC_GA_TRACKING_ID`（`web/src/lib/env.ts`）を削除する
- [ ] 移行後、`.env.production` と `.claude/skills/db-access/SKILL.md` の接続先を
      VPS 上の Supabase（`db.ezocivic.tech`）に切り替える。ローカルの admin（§5 ⑤）もこの接続先を使う

### トレードオフとして引き受けること

1. **月額コスト** — 現状ほぼ0円 → 月数千円の自腹。ただし「自分で負担している」ことは独立性の証明でもある
2. **可用性** — 落ちたら直せるのは自分だけ。非公式・個人運営であることの明示とセットで
3. **Supabase セルフホストの保守** — バックアップ、アップグレード、JWTシークレット管理

---

## 6. C. 管理画面のロール権限

> **詳細設計: [管理画面ロール権限設計](20260912_1736_管理画面ロール権限設計.md)（2026-09-12）**
> 権限マトリクス・DB設計・実装フェーズはそちらを正とする。本節は概要。

### 背景

議員・市長側に管理画面を使ってほしい。目的は2つ。

1. **議案データの妥当性検証と修正** — 当事者なので正確性が分かる
2. **会派見解の移譲** — 今は運営者が各会派の見解を書いているため
   「あの会派の見解は運営者の解釈だ」と言われうる。当の会派が自分で書けばその疑念が消える。
   **プラットフォーマーとして声を選別しないという立ち位置の、直接的な実装**

### 現状

**土台は既にある。**

- ロールは GoTrue の `app_metadata.roles` 配列（`admin/src/lib/auth/permissions.ts:7`）
- `checkEditorPermission` が「将来的に使用」というコメント付きで**空の器として存在**（同 :12-18）
- アカウント発行機能がある（`admin/src/features/admins/server/actions/invite-admin.ts`）。
  ただし運営者がパスワードを決めて直接作る方式で、**メールは送っていない**

**足りないもの:**

- ページ・アクション単位のガード（今は `(protected)` レイアウトで一括保護しているだけ）
- パスワードリセット画面（`admin/src/app` に reset 系ルートが存在しない）
- 招待メール方式と、そのための SMTP（後述の ⑤）
- **ユーザー管理テーブル**（`auth.users` と `get_admin_users()` 関数しかない）

### ロール定義

| ロール | 対象 |
|---|---|
| `admin` | 運営者本人。全権。**アカウント発行は admin のみ** |
| `legislator` | 議員。議案関連の編集＋自会派の見解編集 |
| `candidate` | 出馬者。閲覧のみ |

### 権限マトリクス

| 機能 | admin | 議員 | 出馬者 |
|---|:--:|:--:|:--:|
| 議案データの編集（妥当性検証・修正） | ○ | ○ | − |
| 自会派の見解 編集 | ○ | ○ | − |
| 他会派の見解 編集 | ○ | − | − |
| インタビュー要約レポート 閲覧 | ○ | ○ | ○ |
| インタビュー**生ログ** 閲覧 | ○ | ○ | ○ |
| 議案の公開/非公開 切替 | ○ | − | − |
| 会派・委員会・定例会マスタ | ○ | − | − |
| タグ管理 | ○ | − | − |
| AI情報収集 / 自動生成（Claude CLI） | ○ | − | − |
| アカウント発行・権限変更 | ○ | − | − |
| トピック分析の実行 | ○ | − | − |

### 決定事項と、その帰結

- **議案の編集は直接編集**（承認フローは設けない）。スピード優先。
  → だからこそ**変更履歴が必須**（後述）
- **公開/非公開の切替は admin のみ**。議員に渡すと「不利な議案を隠す」余地が生まれるため
- **インタビューは生ログも含めて議員・出馬者に開示する**
  → **市民への事前告知が必須**。「集計された傾向」ではなく
  **「あなたが書いた内容そのものが議員・立候補予定者に読まれる」と明示する**必要がある。
  `interview_messages`（生ログ）には個人が特定されうる記述が自然に混ざるため、
  アクセス制御ではなく**告知と同意**でカバーする設計になる

### 必須の追加実装

#### ① `admin_profiles` テーブルの新設

会派見解のスコープ制限（自会派のみ編集可）には**ユーザーと会派の紐付け**が要る。
現状 DB にユーザー管理テーブルが無いため新設する。
`app_metadata` に押し込むより、SQL で引けて外部キーが張れるこちらが扱いやすい。

```sql
create table public.admin_profiles (
  user_id uuid primary key references auth.users(id) on delete cascade,
  role text not null check (role in ('admin','legislator','candidate')),
  faction_id uuid references public.factions(id),  -- legislator のみ
  display_name text not null,
  created_at timestamptz not null default now()
);
alter table public.admin_profiles enable row level security;  -- ポリシーは定義しない
```

既存の `checkAdminPermission` / `checkEditorPermission` はこれを見る形に書き換える。

#### ② 変更履歴（監査ログ）

議員が議案データや会派見解を編集できるなら、**誰がいつ何を変えたかが追える**必要がある。

理由は不正防止よりも**疑念の防止**。
他会派から「A党が議案の説明を書き換えたのでは」と言われた時、履歴が無いと反証できない。
**中立プラットフォームを名乗る以上、実質必須。**

#### ③ 権限ガードの実装

- ページ単位の分岐: `bills` / `bills-edit` は議員OK、
  `factions` / `committees` / `admins` / `tags` / `ai-collection` は admin のみ
- ナビゲーションの出し分け
- **Server Action 側にもガードを入れる**（UI を隠すだけでは不十分）

#### ④ パスワードリセット画面

GoTrue の `resetPasswordForEmail` ＋ リセット用ルートを新規実装。

#### ⑤ SMTP

GoTrue がメールを送るための送信サーバー。**今ある機能には要らない**。
現在の `createAdmin()` は運営者がパスワードを決めて直接アカウントを作る方式で、メールを送っていないため。

必要になるのは、議員・出馬者に開く時に作る次の2つ。

1. **招待メール方式**（GoTrue の `inviteUserByEmail`）— 本人が届いたリンクから自分でパスワードを決める
2. **パスワードリセット**（④）— 忘れた時に、運営者を介さず本人が戻せる

どちらも目的は、**運営者が議員のパスワードを知っている状態を作らないこと**。
知っていると、監査ログ（②）に「A議員が編集」と残っても、「運営者がなりすましたのでは」と言われた時に反証できない。
監査ログを必須にした理由（疑念の防止）がここで崩れる。
あわせて、公式の連絡先に招待を送れば、リンクを開けたこと自体が本人確認になる（詳細設計 §8）。

VPS でセルフホストする GoTrue には送信手段が無く、Supabase Cloud の標準の送信もチームメンバー宛てにしか送れないテスト用。
どちらにしても送信用の SMTP を用意する。さくらのメールボックス等、日系で寄せられる。

**必要になる時期**: 議員・出馬者に開放する時（§10 の9番）。公開（§10 の8番）には要らない。

### セキュリティ前提の変化

外部の人（議員・出馬者）が入る以上、**Basic認証で丸ごと隠す運用はできなくなる**。
別途詰める必要がある。

### 作業項目

詳細設計 §9 の実装フェーズ（C-1〜C-4）が正。C-1 は完了（2026-09-12）。

- [x] **`upsertStance` / `deleteStance` の認可チェック漏れを塞ぐ**（C-1）
- [x] `admin_profiles` テーブルのマイグレーション作成（C-1）
- [x] 3ロール対応の認可（C-1。`permissions.ts` は廃止し、`admin_profiles` を正とする `requireRole()` などに置き換え）
- [x] **管理画面で追加した管理者がログインできない不具合の修正** — 管理者追加が `admin_profiles` の行を作らない
      （C-1 以降の既存の不具合。検証結果 §5-2。PR #12）
- [ ] ページ・ナビ・Server Action の権限ガード実装（C-3）
- [ ] 会派見解を「自会派のみ編集可」にスコープ制限（C-3）
- [ ] 変更履歴（監査ログ）の設計と実装（C-2）
- [ ] パスワードリセット画面の実装（C-4）
- [ ] 管理者パスワードの最低文字数の見直し（今は 6。`GOTRUE_PASSWORD_MIN_LENGTH` と管理画面の表示をあわせて変える。検証結果 §4-3）
- [ ] SMTP 設定
- [ ] admin のアカウント発行フロー整備（本人確認の運用をどうするか）
- [ ] 選挙後の権限変更運用（出馬者 → 議員）を決める

---

## 7. D. AI変動費のガード（実装済み / 2026-09-12）

### 問題

**固定費（月数千円）は読めるが、AI 変動費は読めない。**
公開してバズった時に青天井になるのはホスティング代ではなくここ。

### 着手時に見つかった、より大きな穴

当初は「インタビューにもチャットと同じ日次上限を付ける」だけの想定だったが、
**既存のチャットの上限もほぼ機能していなかった**。

- 上限が**ユーザー単位**でしか掛かっていない（`CHAT_DAILY_COST_LIMIT_USD`、既定 $0.5/日）
- しかしログインは**匿名認証**（`signInAnonymously`）。クッキーを消せばユーザーを作り直せる
- つまり $0.5 × ユーザー数 が上限。**総額は事実上いくらでも増える**

読めない請求から実際に守るのは**サイト全体の上限**であり、それが存在しなかった。

### 実装内容

| 項目 | 内容 |
|---|---|
| **全体上限の新設** | `AI_GLOBAL_DAILY_COST_LIMIT_USD`（既定 $5/日）。全ユーザー・全機能の合計で判定 |
| **インタビューの利用記録** | `chat_usage_events` に `metadata.feature = "interview"` で記録。従来は記録ゼロだった |
| **インタビューの上限** | `INTERVIEW_DAILY_COST_LIMIT_USD`（既定 $0.5/日）＋ 全体上限 |
| **デフォルトモデル変更** | インタビュー既定を `gpt-5.2` → `gpt-5-mini`。入力 $1.75→$0.25、出力 $14→$2 の **約1/7** |
| **上限到達時の応答** | インタビューも HTTP 429 ＋「本日の利用上限に達しました」（チャットと同じ文面） |

判定は全体を先に見る。ユーザー単位の上限は「ひとりの暴走」を、全体上限は「請求額」を守る、と役割が違う。

### 実装上の判断

- **集計は DB 側の関数で行う**（`get_ai_usage_cost_usd`）。
  行を全件取得して JS で合算すると PostgREST の `max_rows`（このプロジェクトでは 1000）で
  打ち切られ、**コストを過小評価して上限判定が効かなくなる**。日次集計用のインデックスも追加
- 関数の実行権限は `service_role` のみ（`get_admin_users` と同じ方針）
- **判定処理自体が失敗した場合は続行する（fail-open）**。チャットの既存挙動に合わせた。
  DB 障害時に上限が外れるということなので、**将来見直す候補**
- 純粋関数（`evaluateCostGuard` / `getJstDayRange` / `parseCostLimitUsd`）は
  `web/src/lib/ai/cost-guard.ts` に切り出し、単体テスト 14 件を追加

### 変更ファイル

- `supabase/migrations/20260912072722_add_ai_usage_cost_function.sql`（新規）
- `web/src/lib/ai/cost-guard.ts` / `cost-guard.test.ts`（新規）
- `web/src/lib/env.ts`、`web/src/features/chat/server/{repositories,services}`
- `web/src/features/interview-session/server/services/handle-interview-chat-request.ts`
- `web/src/app/api/interview/chat/route.ts`、`packages/shared/src/ai/models.ts`、`.env.example`

### 残タスク

- [ ] 上限到達時のユーザー向け表示をUIで確認する（429 のハンドリング）
- [ ] fail-open をやめるか判断する
- [ ] 上限到達を運営者が気づく手段（通知）を用意するか判断する

> **本番値の決定は B（VPS構築）側のタスク**。値はリポジトリではなくサーバーの環境変数に置くものであり、
> 1セッションの実コストはローカルのモック（トークン数0）では測れない。
> 公開後に `chat_usage_events` の実績を見て調整する。

---

## 8. 公開前の宿題（法務・運用）

- [ ] **プライバシーポリシー** — インタビュー内容が議員・立候補予定者に開示されること、
      LLM API（国外）に送信されることを明記
- [ ] **利用規約** — 出馬者によるインタビュー内容の選挙運動への利用について線引き。
      **公職選挙法の確認が必要**（要専門家確認、本ドキュメントでは判断しない）
- [ ] **非公式・個人運営であることの明示** — 札幌市公式と誤認されないように
- [ ] 可用性の期待値を明示（個人運営であり無保証であること）
- [ ] AGPL §13 対応（フッターのソースコードリンク維持）

---

## 9. 未決事項

1. Storage をセルフホストするか nginx 静的配信に置き換えるか
2. ~~Langfuse を無料枠維持か、ファイルベース実装で切るか~~ — **切る（2026-09-21）**
3. Vercel AI Gateway を切ってプロバイダ直叩きにするか（機能的には切らなくても動く）
4. ~~Google Analytics を外すか、セルフホスト解析に置換するか~~ — **外す（2026-09-21）**
5. ~~パッケージ名 `@mirai-gikai/*` の改名をやるか~~ — **やった（2026-09-23、PR #14）**。
   `@dejimin-gikai/*` に改名し、GitHub リポジトリ名と Supabase ローカルの `project_id` も揃えた
6. 変更履歴（監査ログ）の粒度と保持期間
7. アカウント発行時の本人確認の運用
8. 公開時期と、公開前にそろえるデータの範囲
9. GitHub Organization（`ezocivic`）を作ってリポジトリを移すか
   — リポジトリ名は `s-takahashi-hokkaido/dejimin-gikai` に変更済み（PR #14）。Organization 化は未決のまま
10. `ezocivic.tech` トップページ（活動体の紹介）をいつ作るか
11. デジ民議会のローマ字・英語表記と、多自治体化の時に取るプロダクト名ドメイン（§4）

---

## 10. 進め方の順序（案）

> **2026-09-17 改訂**: 「公開」と「議員・出馬者への開放」を分け、C-2〜C-4 を公開の後ろへ移した。
> C-2〜C-4 が必要になるのは議員を招く時であって、公開する時ではない。
> C-1 で認可漏れは塞ぎ、Basic認証も入れたので、運営者ひとりで admin を使う間は現状のまま公開できる。
> §1-4 の「完成度より公開の早さ」にも沿う。

1. ~~**D. AIコストガード**~~ — **完了（2026-09-12）**
2. ~~**C-1. ロール基盤とセキュリティ修正**~~ — **完了（2026-09-12）**
3. ~~**A. ドメイン取得**~~ — **完了（Xserverドメインで `ezocivic.tech` を取得）**。
   これ待ちだった DNS・証明書・SMTP・`site.config.ts` に進める
4. ~~**B. ローカルで docker compose 検証**~~ — **完了（2026-09-22）**。本番データでの予行演習のみ残り。
   手順は [ローカル docker compose 検証手順](20260922_1716_ローカルdocker-compose検証手順.md)、
   結果は [ローカル docker compose 検証結果](20260922_2100_ローカルdocker-compose検証結果.md)
   - ~~**検証で見つかった既存の不具合2件を公開前に直す**~~ — **修正済み（PR #12）**（compose とは無関係。検証結果 §5）
     - 下書き・Coming Soon の議案でも `is_featured` が付いているとトップの「注目の議案」に出る
       （`findFeaturedBillsWithContents` だけ `publish_status` で絞り込んでいない）
     - 管理画面で追加した管理者がログインできない（`admin_profiles` の行が作られない）
5. **公開前の法務（§8）** — プライバシーポリシーには**初版から**
   「回答が議員・立候補予定者にそのまま読まれる」「LLM API（国外）に送信される」を書く。
   後から足すと、それ以前に集めた回答を議員に見せる同意が無い状態になる
6. **データの最新化** — 令和8年第3回定例会（9/24〜10/30）の議案を、原案PDFを確認して管理画面から登録する。
   公開時の「現在の定例会」が第2回のままだと価値が落ちる
   - 議案の基本情報（番号・件名・PDF・結果）は[議案等一覧](https://www.city.sapporo.jp/gikai/html/giantouichiran.html)、
     付託委員会は会期後に[本会議の結果](https://www.city.sapporo.jp/gikai/html/shingikekka.html)の審議結果PDFで確認できる
   - 登録前に決めること:
     決算議案の付託先である**決算特別委員会が `committees` マスタに無い** /
     補正予算は**複数の常任委員会に分割付託**されるが `bills.committee_id` は1つしか持てない /
     決算の「認定」を `status` のどれに当てるか
7. **A. リブランド最低限** — サイト名（デジ民議会）・README・「みらい議会」残置の置換
   — **ほぼ完了**（PR #9 OGP・admin ファビコン / #10 影響箇所調査 / #13 表示名・ロゴ・OGP・アイコン /
   #14 パッケージ名・リポジトリ名・`project_id`）。
   残りは **README の書き換え**と、`site.config.ts` の **operator 表記**・`externalLinks` の整理、
   帰属表記（D7）の確定（いずれも §4 の作業項目）
8. **B. VPS 構築・移行・公開** — プロバイダ側の月額ハードリミットと AIコスト上限の本番値もここで設定
9. **C-2〜C-4. 監査ログ → 議員・出馬者に開放 → アカウント運用** — 議員・出馬者に声を掛ける前に必要
10. **A. リブランド仕上げ**（ロゴ・favicon・OGP）

C-2〜C-4 は B と独立しているので、4〜8 と並行して進めてもよい。

### 期限の目安

札幌市議会議員選挙・市長選挙は 2027年4月の統一地方選（前回は 2023年4月）。
candidate ロールを活かすなら、**年明けには議員・出馬者に開放できている**必要があり、
逆算すると**公開は 2026年内**が目安。
出馬者にインタビューを開示する前に公職選挙法の確認（§8、要専門家）も要るため、相談先は早めに当たる。

---

## 関連ドキュメント

- `docs/20260520_1900_札幌市版FORK_GUIDELINES適応手順書.md` — フォーク移行の全体手順
- `docs/20260530_1200_札幌市議会ドメイン知識ガイド.md` — 札幌市議会の制度知識
- `docs/20260516_1830_地方版（北海道議会＋基礎自治体）設計調査.md` — マルチ自治体対応の先行調査
- `docs/20260912_1736_管理画面ロール権限設計.md` — C の詳細設計
- `docs/repository-layer.md` — Repository レイヤーの設計
