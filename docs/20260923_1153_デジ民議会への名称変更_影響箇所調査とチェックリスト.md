# デジ民議会への名称変更：影響箇所の調査とチェックリスト

- 作成日: 2026-09-23
- 対象ブランチ: `develop`（コミット `3975ccfc` 時点）
- 目的: サービス名「みらい議会（札幌市版）」を「**デジ民議会**」に変更するにあたり、旧名称が残っている箇所を洗い出して、変更要否と作業チェックリストを整理する。

---

## 1. 前提と論点（先に決めること）

実装前に次の点を決める。決まらないと文言・ファイル名・URL が確定しない。

| # | 論点 | 選択肢の例 | 現状 |
|---|------|-----------|------|
| D1 | **正式表記**（サイト名 `siteName`） | `デジ民議会` / `デジ民議会＠札幌市` / `デジ民議会 札幌市版` | web は `みらい議会＠札幌市`、admin・keywords・README は `みらい議会ー札幌市版` で**表記が割れている** |
| D2 | **短縮名**（PWA の `short_name`） | `デジ民議会` など、ホーム画面で切れない長さ | `みらい議会＠札幌市` |
| D3 | **ハッシュタグ**（`twitterHashtag`） | `デジ民議会` / `デジ民議会札幌` | `みらい議会札幌市版` |
| D4 | **英字スラッグ**（リポジトリ名・パッケージスコープ・Supabase `project_id`・ドメイン） | `dejimin-gikai` / `digimin-gikai` など | `mirai-gikai-hokkaido`、`@mirai-gikai/*` |
| D5 | **内部識別子まで変えるか** | (a) 表示だけ変える / (b) パッケージ名・リポジトリ名も変える / (c) CSS トークン・DB カラムまで変える | ― |
| D6 | **ロゴ・OGP・PWA アイコン**のデザイン | 文字ロゴ（SVG テキスト）を差し替え / 新規デザイン | 「みらい議会」「み」の文字入り |
| D7 | **「チームみらい」「みらい議会」への帰属表記**の扱い | 「本サービスはチームみらい開発の『みらい議会』をベースにしています」を残す | about・FAQ・footer・README にあり |

### 決定事項（2026-09-23）

| # | 決定 |
|---|------|
| D1 | 正式表記は **`デジ民議会`**（「＠札幌市」「札幌市版」などは付けない）。web・admin・keywords・README をこの表記にそろえる |
| D2 | 短縮名も **`デジ民議会`**（D1 と同じ） |
| D3 | **未決定**（案: `デジ民議会`） |
| D4 | 英字スラッグは **`dezimin-gikai`** |
| D5 | **(b)**：表示名に加えて、リポジトリ名とパッケージ名（`@mirai-gikai/*` → `@dezimin-gikai/*`）も変える。CSS トークンと DB（§3.8）は変えない |
| D6 | ロゴ・OGP・アイコンのうち「みらい」に関係する部分は、すべて「デジ民」にする（例: `logo.svg` と `ogp.svg` の「みらい議会」→「デジ民議会」、favicon・iOS アイコンの「み」→「デ」） |
| D7 | §1「D7 について」の方針案のまま進める（派生元の表記は残す） |

### D7 について（注意）

本リポジトリは **AGPL-3.0**（`LICENSE`）で、チームみらいの「みらい議会」を基にしている。サービス名を変えても、**元の成果物からの派生であることの表記と著作権表示は残す**必要がある。したがって「チームみらい」「みらい議会」という語は**全部消すのではなく**、次のように仕分ける。

- **残す**: 派生元の説明（about の「ベースに作成しています」、FAQ の「公式サービスではありません」、footer の「運営しているものではありません」、`LICENSE`、`CLA.md`）
- **変える**: 自サービスの名前として「みらい議会」を名乗っている箇所
- **消す／見直す**: 自サービスがチームみらいの活動の一部であるかのような文言（「チームみらいから連絡する」など）

### D5 の推奨

**(b) までを段階的に行い、(c) はやらない**ことを推奨する。

- `mirai-text` などの CSS カラートークン（約 400 箇所）と、DB テーブル `mirai_stances`・型 `MiraiStance` は、利用者の目に触れない。変えると差分が大きく、上流（福岡市版・本家）からの取り込みもしにくくなる。
- パッケージスコープ `@mirai-gikai/*` とリポジトリ名は外から見えるが、変えるとインポート文やスクリプトが広く変わるため、表示名の変更とは**別の PR** に分ける。

---

## 2. 調査方法

```bash
git grep -n -I -E "みらい議会|チームみらい|札幌版|Team Mirai|mirai" \
  -- ':!docs/schema' ':!pnpm-lock.yaml'
```

- ヒットは約 230 ファイル。そのうち大半は CSS トークン（`text-mirai-text` など）と `@mirai-gikai/*` の import。
- 次のものは**名称変更とは無関係**なので対象外とした。
  - `mirai-sapporo`（会派「未来さっぽろ」のスラッグ。`.claude/skills/update-bills/SKILL.md` など）
  - `tmp_council_page.html` 内の「みらい川崎市議会議員団」（川崎市の会派名。ファイル自体が不要な一時ファイル）
  - `web/public/icons/about-typography.svg`（中身は英字 “About” のパス。名称は含まない）
- DB に入っているデータや、Langfuse のプロンプトなど**リポジトリの外にある箇所**は `git grep` では見つからないため、§3.6 に「要確認」としてまとめた。

---

## 3. 影響箇所一覧

### 3.1 サイト名の設定（ここを変えると多くの画面に反映される）

`siteConfig.siteName` は、ヘッダー、フッター、メタデータ（title / OGP / Twitter）、利用規約、FAQ、トップの about、チャットの提案文、インタビュー同意文など **20 箇所以上から参照**されている。まず設定ファイルを直すのが近道。

| ファイル | 箇所 | 現在の値 |
|---------|------|---------|
| `web/src/config/site.config.ts:7` | `siteName` | `みらい議会＠札幌市` |
| `web/src/config/site.config.ts:13` | `keywords[0]` | `みらい議会ー札幌市版` |
| `web/src/config/site.config.ts:25` | `twitterHashtag` | `みらい議会札幌市版` |
| `web/src/config/site.config.ts:29-33` | `externalLinks.donation / teamAbout / terms / privacy / faq` | `team-mir.ai` / `team-mirai.notion.site` を指す（§3.3 参照） |
| `web/src/config/site.config.ts:37, 60` | コメント | 「チームみらい」の例示（コード上の説明なので変更は任意） |
| `web/src/config/site.config.ts:64` | `features.showTeamMiraiSection` | `false`（現状は非表示） |
| `admin/src/config/site.config.ts:6` | `siteName` | `みらい議会ー札幌市版`（admin の見出し「… Admin」に使われる） |
| `web/public/manifest.json:2-3` | `name` / `short_name` | `みらい議会＠札幌市` |

**`siteName` を参照している主な箇所**（設定を変えれば自動で変わる。文として不自然にならないかだけ確認）

- `web/src/app/layout.tsx:25-46`（title、OGP の alt・siteName、Twitter card）
- `web/src/components/header/header-client.tsx:37,43`、`web/src/components/layouts/desktop-menu/logo.tsx:18,36`、`.../action-buttons.tsx:19`（「{siteName}とは」）
- `web/src/components/layouts/footer/footer.tsx:33,36`
- `web/src/components/top/about.tsx:22,35,51`、`coming-soon-section.tsx:20`、`team-mirai.tsx:43`
- `web/src/app/(main)/terms/page.tsx:12-54`、`web/src/app/faq/page.tsx:12-144`
- `web/src/features/chat/client/components/chat-window.tsx:104`（「{siteName}って何？」の提案チップ）
- `web/src/features/interview-config/client/components/interview-lp-page.tsx:163`、`interview-consent-modal.tsx:125`
- `web/src/features/bills/client/utils/share-handlers.ts:36`、`share.ts:23`（ハッシュタグ）
- `admin/src/app/(protected)/layout.tsx:29`

### 3.2 画面にハードコードされている文言

`siteConfig` を通さず、ソースに直接書かれている箇所。**設定を変えても直らない**ので個別に対応する。

| ファイル:行 | 文言 | 対応方針（案） |
|------------|------|---------------|
| `web/src/features/interview-config/server/components/interview-disclosure-page.tsx:82` | 回答データは「みらい議会」上に掲載され… | **変更**（`siteConfig.siteName` を使う） |
| `web/src/components/top/about.tsx:60` | このサイトは「チームみらい」開発の「みらい議会」をベースに作成しています。 | **残す**（派生元の表記。D7） |
| `web/src/components/top/about.tsx:73,85` | 「チームみらい」について／本家「みらい議会」（国会版）を見に行く | **残す**（派生元へのリンク） |
| `web/src/components/top/about.tsx:92` | 「チームみらい」の公式ではない、非公式のサイトです。 | **残す** |
| `web/src/app/faq/page.tsx:32-38,97` | チームみらいの公式サービスですか？ ほか | **残す**（ただし新名称で文が通るか確認） |
| `web/src/components/layouts/footer/footer.tsx:97` | このサービスは政党チームみらいが運営しているものではありません | **残す** |
| `web/src/components/layouts/footer/footer.tsx:105` | © 2025 Team Mirai All rights Reserved | 現在 `showTeamMiraiSection=false` で非表示。表示するなら**自サービスの表記に変更** |
| `web/src/components/top/hero.tsx:27` | powered by Team Mirai & AI | 同上（現在は非表示） |
| `web/src/components/top/team-mirai.tsx`（全体） | チームみらいについて／寄付リンク | 現在は非表示。**削除するか**、残すかを決める |
| `web/src/components/layouts/desktop-menu/links.tsx:14`、`footer/footer.config.ts:32` | 「チームみらいについて」メニュー | 同上（`showTeamMiraiSection` で制御） |
| `web/src/features/interview-report/client/components/expert-registration-banner.tsx:24` | 今後チームみらいから追加のインタビューを… | **変更**（連絡してくるのは本サービスの運営者。`siteConfig.managingParty` か運営者名に） |
| `web/src/features/interview-report/client/components/expert-registration-modal.tsx:102,124` | 有識者としてチームみらいから連絡をする可能性が… | **変更**（同上） |
| `admin/src/app/login/page.tsx:35` | © 2025 チームみらい. All rights reserved. | **変更**（admin のログイン画面） |
| `admin/src/features/interview-config/client/components/interview-config-form.tsx:399` | 「…チームみらいの仮説などの情報を入力」（placeholder） | **変更**（「運営の仮説」など） |
| `web/src/app/(main)/page.tsx:120,123` | コメント「みらい議会とは」「チームみらいについて」 | 任意（コードコメント） |
| `web/src/app/layout.tsx:61` | TODO コメント「札幌版の GSC 認証コード」 | 任意 |

### 3.3 外部リンク・SNS

| ファイル | 内容 | 対応方針（案） |
|---------|------|---------------|
| `web/src/config/site.config.ts:29` `donation` | `https://team-mir.ai/support/donation` | デスクトップメニューとフッターで使われる。**自サービスに寄付窓口がなければ空にして非表示に** |
| `web/src/config/site.config.ts:30` `teamAbout` | `https://team-mir.ai/about` | `showTeamMiraiSection` と連動。方針に合わせる |
| `web/src/config/site.config.ts:31-33` `terms / privacy / faq` | team-mir.ai・notion を指す | **現在どこからも参照されていない**（利用規約・FAQ は自前ページ）。削除候補 |
| `web/src/lib/social-links.ts` | チームみらいの Web / YouTube / X / LINE / Instagram / Facebook | `team-mirai.tsx` などで使われる。自サービスの SNS に差し替えるか削除 |
| `web/src/components/top/team-mirai.tsx:51,63` | `https://team-mir.ai/` と寄付リンク | 同上 |
| `web/src/components/top/about.tsx:65,77` | `team-mir.ai` / `gikai.team-mir.ai` | **残す**（派生元へのリンク） |
| `web/src/config/site.config.ts:28` `report` | Google フォーム（不具合報告） | フォームのタイトルに旧名称がないか**Google フォーム側を確認** |

### 3.4 画像・アセット（ファイルの中に名称が入っている）

| ファイル | 中身 | 使われている場所 |
|---------|------|-----------------|
| `web/public/img/logo.svg` | テキスト「みらい議会」「＠札幌市」 | ヘッダー、フッター、デスクトップメニュー、about |
| `web/public/ogp.svg` | 「みらい議会」「（非公式）　札幌市版」 | 全ページの OGP、議案詳細・インタビューの既定 OGP |
| `web/public/icons/pwa/icon_sapporo.svg` | 「み」の一文字 | favicon（`layout.tsx:34`）、PWA アイコン |
| `web/public/icons/pwa/icon_ios.png` | 「み」の一文字（画像） | apple-touch-icon（`layout.tsx:35`） |
| `web/public/icons/pwa/icon_android_192.png` / `_512.png` / `icon_dev_192_v3.png` | 未参照（要目視確認） | 現在どこからも参照されていない。不要なら削除 |
| `web/public/icons/team-mirai-typography.svg` | チームみらいのロゴ | `team-mirai.tsx`（非表示中） |
| `web/public/favicon.ico` | 要目視確認 | Next.js が自動で配信 |

> OGP の `ogp.svg` は、X や Facebook など **SVG の OGP 画像を表示しない SNS** がある。差し替えのついでに PNG（1200×630）にすることも検討する。

### 3.5 テスト

| ファイル | 内容 |
|---------|------|
| `web/src/features/bills/client/utils/share.test.ts:38,47,57,60,62` | ハッシュタグ `#みらい議会札幌市版` を期待値に直接書いている。D3 が決まったら**期待値を更新**する（`siteConfig.twitterHashtag` を参照する形にすると次から楽） |

### 3.6 リポジトリの外（git grep では見つからない。要確認）

| 対象 | 確認すること |
|------|-------------|
| **本番 DB のデータ** | `bill_contents`（解説文）、`interview_configs`（テーマ・仮説）、`general_questions`、`press_conferences` などの本文に「みらい議会」「チームみらい」が含まれていないか。`ilike '%みらい%'` で検索する（`db-access` スキルの手順に従う） |
| **Langfuse のプロンプト** | AI チャット・AI インタビューのシステムプロンプトは Langfuse で管理されている（`web/src/lib/prompt/langfuse/`）。プロンプト本文に「みらい議会」「チームみらい」が入っていないか確認し、必要なら新しいバージョンを作る |
| **Vercel** | プロジェクト名、本番ドメイン、`NEXT_PUBLIC_APP_URL` / `NEXT_PUBLIC_WEB_URL` / `ADMIN_URL` |
| **Supabase（クラウド）** | プロジェクト名、Auth のメール送信者名・テンプレート（`sender_name`）、Site URL・Redirect URL |
| **GitHub** | リポジトリ名 `s-takahashi-hokkaido/mirai-gikai-hokkaido`、About の説明文、Topics |
| **Google Search Console** | 新ドメインにするなら登録し直し（`layout.tsx:61` の TODO） |
| **Google フォーム**（不具合報告） | フォームのタイトル・説明文 |
| **SNS・告知** | X のプロフィール、固定ポスト、ハッシュタグの周知 |

### 3.7 リポジトリ・パッケージの識別子（D5 で (b) を選んだ場合）

| 対象 | 箇所 | 備考 |
|------|------|------|
| ルートパッケージ名 | `package.json` の `@mirai-gikai/root` | |
| ワークスペースパッケージ | `@mirai-gikai/supabase`（約 84 箇所で import）、`@mirai-gikai/shared`、`@mirai-gikai/seed` | 変えるなら `package.json` の `name`、全 import、`web/package.json`・`admin/package.json`・`packages/seed/package.json` の依存、ルートの `seed` スクリプト（`--filter @mirai-gikai/seed`）、`pnpm-lock.yaml` を一括で直す |
| Supabase ローカル | `supabase/config.toml:5` `project_id = "mirai-gikai-hokkaido"` | 変えるとローカルの Docker コンテナ名・ボリューム名が変わり、**ローカル DB を作り直すことになる** |
| worktree の命名規則 | `CLAUDE.md`・`AGENTS.md`・`.claude/commands/codify.md`・`.claude/skills/parallel-pr/SKILL.md` の `../mirai-gikai-<branch>` | 作業手順の文字列。リポジトリ名に合わせるなら変更 |
| `.gitignore:20` | `mirai-gikai-upstream/` | 上流の参照用ディレクトリ名。上流の名前なので**変えない** |
| README のバッジ | `README.md:21-22` の DeepWiki / codecov | 本家リポジトリを指している。自リポジトリ用に差し替えるか削除 |

### 3.8 変えないもの（内部識別子）

利用者の目に触れず、変えると差分が大きいもの。**今回は変更しない**ことを推奨する。

| 対象 | 規模 |
|------|------|
| CSS カラートークン `--color-mirai-*`、`text-mirai-text`、`bg-mirai-surface` など | `web/src/app/globals.css` の定義 34 箇所＋利用側約 400 箇所 |
| DB テーブル `mirai_stances`、型 `MiraiStance`、関数 `findMiraiStanceByBillId` など | マイグレーション・型生成・repository にまたがる |
| `siteConfig.features.showTeamMiraiSection`、`TEAM_MIRAI_SNS_ORDER`、`team-mirai.tsx` | 機能フラグ名・コンポーネント名 |
| テスト用の ID `mock-mirai-stance-*`（`tests/supabase/utils.ts:274`） | テスト内部 |
| 会派スラッグ `mirai-sapporo` | 会派「未来さっぽろ」。サービス名とは無関係 |

### 3.9 ドキュメント・シードデータ

| 対象 | 対応方針（案） |
|------|---------------|
| `README.md:1,7,19` | タイトルを新名称に。「チームみらいの『みらい議会』を Fork」の記述は**残す** |
| `CLA.md` | チームみらいの CLA をそのまま持ってきたもの。自リポジトリで CLA を求めるか決め、不要なら削除、必要なら主体を書き換え |
| `CLAUDE.md`・`AGENTS.md`・`GEMINI.md` | 名称の記述があれば新名称へ（worktree 命名は §3.7） |
| `docs/` 配下の過去の設計書 | **変更しない**（当時の記録として残す。命名ルール上も新規ファイルで追記する方針） |
| `docs/schema/` | 生成物なので手で編集しない |
| `packages/seed/csv/data/bill_contents_rows.csv`、`interview_configs_rows.csv` | 本家（国会版）のサンプルデータ。「みらい議会：[…](https://gikai.team-mir.ai/…)」は国会版へのリンクなので**変更不要**。「## チームみらいの仮説」は開発用データなので任意 |
| `packages/seed/main/data.ts` | 12 箇所ヒット（中身は `mirai_stances` など内部識別子）。変更不要 |

---

## 4. チェックリスト

### フェーズ 0: 方針決定

- [x] D1 正式表記（`siteName`）を決める → `デジ民議会`
- [x] D2 短縮名（PWA `short_name`）を決める → `デジ民議会`
- [ ] D3 ハッシュタグを決める
- [x] D4 英字スラッグを決める → `dezimin-gikai`
- [x] D5 内部識別子をどこまで変えるか決める → リポジトリ名・パッケージ名まで変える。CSS トークンと DB は変えない
- [x] D6 ロゴ・OGP・アイコンの方針を決める → 「みらい」関係の部分をすべて「デジ民」にする
- [ ] D7 「チームみらい」「みらい議会」の帰属表記で残すもの・変えるもの・消すものを確定する（§3.2 の方針案を確認）

### フェーズ 1: 表示名の変更（PR 1）

**設定**
- [ ] `web/src/config/site.config.ts` の `siteName` / `keywords` / `twitterHashtag`
- [ ] `admin/src/config/site.config.ts` の `siteName`
- [ ] `web/public/manifest.json` の `name` / `short_name`
- [ ] `siteConfig.externalLinks` の未使用項目（`terms` / `privacy` / `faq`）を整理
- [ ] `siteConfig.externalLinks.donation` / `teamAbout` の扱いを決めて反映

**ハードコードされた文言**
- [ ] `interview-disclosure-page.tsx:82` の「みらい議会」→ `siteConfig.siteName`
- [ ] `expert-registration-banner.tsx:24` の「チームみらいから」→ 運営者表記
- [ ] `expert-registration-modal.tsx:102,124` の「チームみらいから」→ 運営者表記
- [ ] `admin/src/app/login/page.tsx:35` のコピーライト
- [ ] `admin/.../interview-config-form.tsx:399` の placeholder
- [ ] `footer.tsx:105` のコピーライト、`hero.tsx:27` の powered by（表示する場合）
- [ ] `team-mirai.tsx`・`social-links.ts`・`team-mirai-typography.svg` を残すか削除するか反映
- [ ] about・FAQ・footer の帰属表記が新名称と並んでも文として自然か確認

**画像**
- [ ] `web/public/img/logo.svg` を差し替え
- [ ] `web/public/ogp.svg` を差し替え（PNG 化も検討）
- [ ] `web/public/icons/pwa/icon_sapporo.svg`（favicon）を差し替え
- [ ] `web/public/icons/pwa/icon_ios.png`（apple-touch-icon）を差し替え
- [ ] `web/public/favicon.ico` を確認・差し替え
- [ ] 未参照の `icon_android_*.png` / `icon_dev_192_v3.png` を確認し、不要なら削除

**テスト**
- [ ] `share.test.ts` のハッシュタグ期待値を更新

**ドキュメント**
- [ ] `README.md` のタイトル・説明（派生元の記述は残す）
- [ ] `CLA.md` の扱い
- [ ] `CLAUDE.md` / `AGENTS.md` / `GEMINI.md` の名称記述

**確認**
- [ ] `git grep -n "みらい議会"` の残りが「残す」と決めた箇所だけになっている
- [ ] `git grep -n "チームみらい"` の残りが「残す」と決めた箇所だけになっている
- [ ] `pnpm lint` / `pnpm typecheck` / `pnpm test` が通る
- [ ] `pnpm dev` で次の画面を目で確認する
  - [ ] トップ（ヘッダーのロゴ、about、フッター）
  - [ ] デスクトップメニュー（「{siteName}とは」）
  - [ ] 議案詳細（シェアボタンのハッシュタグ・シェア文）
  - [ ] AI チャット（提案チップ「{siteName}って何？」）
  - [ ] インタビュー LP・同意モーダル・開示ページ・有識者登録バナー・モーダル
  - [ ] 利用規約・FAQ
  - [ ] ブラウザタブのタイトルと favicon
  - [ ] admin のログイン画面とヘッダー
- [ ] OGP をデバッガー（X Card Validator 相当、Facebook シェアデバッガー等）で確認
- [ ] スマホで「ホーム画面に追加」したときの名前とアイコンを確認

### フェーズ 2: リポジトリの外（PR とは別に作業）

- [ ] 本番 DB の本文データに旧名称がないか検索し、あれば修正案を作ってユーザー確認のうえ更新（CLAUDE.md「AI 生成コンテンツの DB 更新ルール」に従う）
- [ ] Langfuse のプロンプト本文を確認し、必要なら新バージョンを作る
- [ ] Vercel のプロジェクト名・ドメイン・環境変数（`NEXT_PUBLIC_APP_URL` / `NEXT_PUBLIC_WEB_URL` / `ADMIN_URL`）
- [ ] Supabase（クラウド）のプロジェクト名、Auth のメール送信者名・テンプレート、Site URL・Redirect URL
- [ ] ドメインを変える場合: 旧ドメインからのリダイレクト、Google Search Console の登録
- [ ] 不具合報告用 Google フォームのタイトル・説明
- [ ] SNS のプロフィール・告知

### フェーズ 3: リポジトリ・パッケージ名の変更（PR 2、D5 で (b) を選んだ場合）

- [ ] GitHub リポジトリ名を変更（旧 URL は GitHub が自動でリダイレクトする）し、`git remote set-url origin` を更新
- [ ] `@mirai-gikai/*` のパッケージ名・import・依存・スクリプトを一括変更し、`pnpm install` で `pnpm-lock.yaml` を更新
- [ ] `supabase/config.toml` の `project_id`（ローカル DB を作り直す手順を README に書く）
- [ ] worktree の命名規則（`CLAUDE.md`・`AGENTS.md`・`codify.md`・`parallel-pr/SKILL.md`）
- [ ] README のバッジ（DeepWiki / codecov）
- [ ] `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm test:integration` が通る
